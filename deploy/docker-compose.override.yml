# Host port overrides for shared server (ishifo-minio uses 9000, nginx uses 8080)
services:
  minio:
    ports:
      - "127.0.0.1:9100:9000"
      - "127.0.0.1:9101:9001"

  api:
    ports:
      - "127.0.0.1:18080:8080"
